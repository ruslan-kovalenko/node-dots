{
  "name": "node-dots",
  "version": "1.0.0",
  "description": "server for dots",
  "main": "server.ts",
  "scripts": {
    "start": "ts-node --esm server/server.ts",
    "dev": "nodemon server/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ruslan",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "openai": "^4.8.0",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "@types/node": "^20.6.2",
    "nodemon": "^2.0.20",
    "typescript": "^5.2.2"
  }
}
